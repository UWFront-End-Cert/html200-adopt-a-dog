//5 element array
let colorsArray = ['white', 'blue', 'green', 'red', 'yellow'];

//print array elements, including the index and a comma, to console uwing a for loop
for (i=0; i<colorsArray.length; i++)
{
    console.log(i + ',' + colorsArray[i]);
}