
function info(name, Dogs, price) {
  alert(name + ',' + ' ' + Dogs + ',' + ' ' + price);
  }

let total = 0;

function dogprice(price) {
  total = price + total;
  alert('Total:' + ' ' + '$' + total);
}
