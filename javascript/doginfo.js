function adoptionFee(name, breed, price) {
  alert(name + ',' + ' ' + breed + ',' + ' ' + price);
  }

let total = 0;

function adoptionFee(price) {
  total = price + total;
  alert('Total:' + ' ' + '$' + total);
}
