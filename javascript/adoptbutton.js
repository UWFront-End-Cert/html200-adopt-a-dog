let Final = 0;

function AddtoCart(value) {
    Final = Final + value;
    alert("Adding " + value + "to cart.  The total adoption fee is now: " + Final);
};

function Dog(Name,Breed,Fee) {
    alert(Name + "(" + Breed + ") has an adoption fee of " + Fee);
};
