function showInfo(name, breed, cost) {
  alert(name + ', ' + breed + ', '+ 'is cost '+ cost);
}


let total = 0;

function addTotal(x) {
  total += Number(x);
  alert('Your new total is: $' + total);
}
